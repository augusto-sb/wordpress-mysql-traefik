```
put '127.0.0.1 www.augusto.com' in /etc/hosts
enter https://www.augusto.com
```

```
if you have a public ip and domain you are ready to go with letsencrypt
```

```
no hay ningun not found o link a http:// o ip en vez de dominio...
```

``` bash
docker compose -f docker-compose.yml.test up # test
docker compose -f docker-compose.yml up --detach # prod
```